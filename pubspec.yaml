name: crashy
description: A Flutter app that knows how to crash in various ways.
environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  sentry: 2.2.0
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
